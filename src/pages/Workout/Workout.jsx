import Stream from '../Stream/Stream';
// import workout from './workout.json';

export default function Workout() {
  // console.log(workout);
  // let name = "bicep_curls";
  // const vpts = new Set(["bicep_curls"]);
  // ne
  return (
    <div>
      {/* <Stream name={name} vpt={vpts.has(name)} nextRep={nextRep} /> */}
      <Stream />
    </div>
  );
}